# Handwritten-Digit-Recognition-CNN
Handwritten digit recognition system using a CNN trained on the MNIST dataset. Includes a robust preprocessing pipeline and a Tkinter-based GUI that allows users to draw digits and receive real-time predictions with confidence scores, demonstrating an end-to-end deep learning workflow.
